{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Natural Events Formula Engine Configuration Schema",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["dry_run", "measurements"],
      "description": "Execution mode: dry_run bypasses GUI/CLI interactivity and file writes; measurements executes full interactive/automated pipeline."
    },
    "paths": {
      "type": "object",
      "properties": {
        "raw_folder": {
          "type": "string",
          "description": "Path to the folder containing raw input images."
        },
        "processed_folder": {
          "type": "string",
          "description": "Path to the folder where processed images will be saved."
        },
        "output_csv": {
          "type": "string",
          "description": "Path to the CSV file where measurement results will be written."
        }
      },
      "required": ["raw_folder", "processed_folder", "output_csv"]
    },
    "grid": {
      "type": "object",
      "properties": {
        "cell_width_px": {
          "type": "number",
          "minimum": 1,
          "description": "Width of each grid cell in pixels."
        },
        "cell_height_px": {
          "type": "number",
          "minimum": 1,
          "description": "Height of each grid cell in pixels."
        },
        "text_size": {
          "type": "number",
          "minimum": 1,
          "description": "Font size for grid cell labels."
        },
        "text_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Hex color code for text labels (e.g., #FFFF00)."
        },
        "grid_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Hex color code for grid lines."
        },
        "grid_thickness": {
          "type": "number",
          "minimum": 1,
          "description": "Thickness of grid lines in pixels."
        }
      },
      "required": [
        "cell_width_px",
        "cell_height_px",
        "text_size",
        "text_color",
        "grid_color",
        "grid_thickness"
      ]
    }
  },
  "required": ["paths", "grid", "mode"]
}